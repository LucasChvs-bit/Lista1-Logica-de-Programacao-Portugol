Algoritmo "Crescente"

Var

  X, Y : inteiro

Inicio

  escreval("Digite dois numeros:")
    leia(X)
    leia(Y)

  enquanto X <> Y faca

    se X > Y entao
      escreval("CRESCENTE!")
    senao
      escreval("DECRESNETE!")
    fimse

  escreval("Digite dois numeros:")
    leia(X)
    leia(Y)

  fimenquanto
    

Fimalgoritmo